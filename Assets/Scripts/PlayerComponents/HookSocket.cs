using UnityEngine;

namespace PlayerComponents
{
    public class HookSocket : MonoBehaviour
    {
        // private new SpriteRenderer renderer;
        private readonly Color colorOn = Color.yellow;
        private readonly Color colorOff = Color.white;
        // private void Awake() => renderer = GetComponent<SpriteRenderer>();
        // private void Start() => TurnOff();
        // public void TurnOn() => renderer.color = colorOn;
        // public void TurnOff() => renderer.color = colorOff;
    }
}
